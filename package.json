{
  "name": "@phlens/noesis",
  "version": "0.1.0",
  "description": "Noesis learning control plane utilities.",
  "bin": {
    "noesis": "bin/noesis"
  },
  "files": [
    "bin/",
    "lib/",
    "skills/",
    "examples/learning-event.example.json",
    "examples/noesis-config.example.toml",
    "examples/promote-request.example.json",
    "docs/",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "check": "node --check bin/noesis && node --check lib/*.mjs && node --check tests/*.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@phlens/pamem": "git+ssh://git@github.com/PHLens/pamem.git"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}
